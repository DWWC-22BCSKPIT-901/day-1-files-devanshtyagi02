#include<iostream>
using namespace std;
#include <iostream>
using namespace std;

int main() {
    // Input: Read the integer n
    int n;
    cin >> n;

    // Calculate the sum using the formula
    int sum = n * (n + 1) / 2;

    // Output: Print the result
    cout << sum << endl;

    return 0;
}